docker build -t ginoconexion/cdb-mysql .

docker run -d --name=cdb-mysql --net=cdb-net -e MYSQL_ROOT_PASSWORD=root ginoconexion/cdb-mysql

docker start cdb-mysql

docker exec -it some-mysql /bin/bash

mysql -p

root

-> visualisation de la base déployée 
